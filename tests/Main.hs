{-# LANGUAGE TypeApplications #-}

module Main where

import           Prelude                                     hiding (Fractional (..), Num (..))

main :: IO ()
main = do
    putStrLn "\nAll tests passed!"
